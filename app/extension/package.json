{
  "name": "agent-quest",
  "displayName": "Agent Quest",
  "description": "Your AI coding sessions, gamified into a pixel-art RPG — a live companion panel for Claude Code, Codex & Cursor.",
  "version": "0.0.1",
  "publisher": "NattaP",
  "license": "MIT",
  "icon": "media/icon.png",
  "categories": [
    "Visualization",
    "Other"
  ],
  "keywords": [
    "claude",
    "claude code",
    "codex",
    "cursor",
    "ai agent",
    "rpg",
    "gamification",
    "pixel art",
    "hud",
    "productivity"
  ],
  "galleryBanner": {
    "color": "#241a33",
    "theme": "dark"
  },
  "sponsor": {
    "url": "https://buymeacoffee.com/natta_p"
  },
  "homepage": "https://github.com/Hin-Nattapat/agent-quest#readme",
  "repository": {
    "type": "git",
    "url": "https://github.com/Hin-Nattapat/agent-quest"
  },
  "bugs": {
    "url": "https://github.com/Hin-Nattapat/agent-quest/issues"
  },
  "engines": {
    "vscode": "^1.90.0"
  },
  "main": "./dist/extension.js",
  "activationEvents": [],
  "contributes": {
    "viewsContainers": {
      "panel": [
        {
          "id": "agentQuestPanel",
          "title": "Agent Quest",
          "icon": "media/icon.svg"
        }
      ]
    },
    "views": {
      "agentQuestPanel": [
        {
          "id": "agentQuest.companion",
          "type": "webview",
          "name": "Companion"
        }
      ]
    },
    "commands": [
      {
        "command": "agentQuest.openCompanion",
        "title": "Agent Quest: Open Companion"
      }
    ]
  },
  "scripts": {
    "build": "node esbuild.mjs",
    "copy-webview": "node scripts/copy-webview.mjs",
    "build:all": "npm --prefix .. run build && npm run copy-webview && npm run build",
    "package": "npm run build:all && vsce package --no-dependencies",
    "reinstall": "npm run package && code --install-extension \"$(ls -t agent-quest-*.vsix | head -1)\" --force",
    "publish": "npm run build:all && vsce publish --no-dependencies",
    "publish:ovsx": "npm run package && ovsx publish \"$(ls -t agent-quest-*.vsix | head -1)\"",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^25.0.0",
    "@types/vscode": "^1.90.0",
    "@vscode/vsce": "^3.9.2",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0"
  }
}
